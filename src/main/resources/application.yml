spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/board
    username: sa
    password: 1
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true          #system.out?? ???.
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug     #log? ?? hibernate? ???? sql? ? ??.
#    org.hibernate.type: trace    #?? ???? ?? ???
